services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=authorization-server-db'
      - 'POSTGRES_PASSWORD=bimatnha'
      - 'POSTGRES_USER=phatdo'
    ports:
      - '15432:5432'
